/** Width of each character symbol, in pixels. */
#define GLYPH_WIDTH 18

/** Height of each character symbol, in pixels. */
#define GLYPH_HEIGHT 36

/** Representation of the 95 printable ASCII characters, in order starting
    from the space, each one taking LTR_HEIGHT consecutive rows. */
extern short shapes[][ GLYPH_WIDTH ];
